import { defineComponent } from 'vue';

/**
 * A container for placing buttons that becomes sticky at the bottom of the page
 * on mobile.
 */
export default defineComponent({
  name: 'NeonStickyButtons',
});
