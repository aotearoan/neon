import { Component, Vue } from 'vue-property-decorator';

/**
 * A card header section, used to contain titles and secondary actions (e.g. favorite button). This is normally the first section in a card but may also be placed below other sections, e.g. a full width section containing an image.
 */
@Component
export default class NeonCardHeader extends Vue {}
