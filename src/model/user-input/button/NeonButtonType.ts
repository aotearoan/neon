/**
 * Defines the possible button type values, these correlate to the type attribute on an HTML <button>.
 * @enum
 */
export enum NeonButtonType {
  Button = 'button',
  Submit = 'submit',
  Reset = 'reset',
}
